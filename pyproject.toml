[project]
name = "ai-infra-seminars"
version = "0.1.0"
description = "AI Infra Seminars assignments (CUDA/Triton kernels)"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pytest>=8.0.0",
    "tilelang>=0.1",
    "torch>=2.13.0",
    "triton>=3.0",
]
